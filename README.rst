Introduction
============

My home settings.

.. DANGER::
   For personal use only.


Installation
------------

Debian based systems:

::

    $ git clone https://github.com/klen/.home.git --recursive ~/.home
    $ sudo apt-get update
    $ cd .home && make
