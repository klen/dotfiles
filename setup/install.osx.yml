---

- homebrew: update_homebrew=yes
  become: no

- homebrew:
    name:
      - bash
      - bash-completion
      - coreutils
      - ctags
      - docker
      - git
      - git-flow
      - golang
      - htop-osx
      - node
      - postgresql
      - pyenv
      - terminal-notifier
      - unzip
      - vim
      - wget
      - yarn
  become: no
